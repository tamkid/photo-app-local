import React from 'react';
import { Alert } from 'reactstrap';

NotFound.propTypes = {
    
};

function NotFound(props) {
    return (
        <Alert color="warning">
            Oop!...Not Found
        </Alert>
    );
}

export default NotFound;